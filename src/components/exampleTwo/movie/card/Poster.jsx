import React from 'react';
import PropTypes from 'prop-types';

const Poster = ({ path }) => {
  return (
    <div>
      
    </div>
  )
}

Poster.propTypes = {
  path: PropTypes.string
}

export default Poster;